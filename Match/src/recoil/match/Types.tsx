export interface MatchListTypes {
  matchId: number;
  time: string;
  name: string;
  type: string;
  status: string;
}

export interface FavoriteItem {
  favoriteId: number;
  placeId: number;
  region: string;
  name: string;
  address: string;
}
