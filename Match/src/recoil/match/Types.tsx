export interface MatchListTypes {
  matchId: number;
  time: string;
  name: string;
  type: string;
  status: string;
}

export interface FavTypes {
  favId: number;
  name: string;
  place: string;
}
